// use that API to load the google maps API
// the benefit of doing it this way is that the Ajax API
// defines google.loader.ClientLocation based on the user's
// IP (and possibly other ways in the future, if available)
//google.load("maps","3", {other_params:"sensor=false"});
//google.load("jquery", "1.7");
//google.setOnLoadCallback(initialize);
